## Setting Up Caddy Web Server with Systemd ##

* Clone the repository
* Run `provision.sh` script. Make sure that you have sudo privileges
* Then replace `Caddyfile` in `/etc/caddy` location
* Restart systemd script
